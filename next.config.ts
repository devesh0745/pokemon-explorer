import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  /* config options here */
  images: {
    domains: ['raw.githubusercontent.com'], // Add raw.githubusercontent.com here
  },
};

export default nextConfig;
